'use client';

import Workspace from '@/components/Workspace';

export default function Page() {
  return (
    <main className="h-full w-full">
      <h1 className="sr-only">Actuarial Reserving Platform AI Workspace</h1>
      <Workspace />
    </main>
  );
}

